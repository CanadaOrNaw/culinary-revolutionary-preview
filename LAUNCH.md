# Launch state — culinary-revolutionary.com

Updated 2026-09-10. This status supersedes the earlier transfer/preview runbook.

## Confirmed

- Repository: `CanadaOrNaw/culinary-revolutionary-preview`; deployment branch: `main`.
- The user completed the Namecheap transfer and created the business mailbox.
- Production: https://culinary-revolutionary.com/ — GitHub Pages, HTTP 200.
- `www.culinary-revolutionary.com` redirects to the apex.
- GitHub Pages certificate covers both hostnames; HTTPS is enforced.
- Authoritative nameservers: `dns1.registrar-servers.com`, `dns2.registrar-servers.com`.
- MX: `mx1.privateemail.com` and `mx2.privateemail.com`, both priority 10.
- SPF: `v=spf1 include:spf.privateemail.com ~all`.
- Namecheap Private Email Launch, one mailbox: `chef@culinary-revolutionary.com`.
- All seven menus / 240 dishes are implemented; generated by `build-menus.py`.
- Public deployment excludes internal source documents and build notes.
- DKIM published at `privateemail._domainkey`; both authoritative nameservers and
  Google DNS returned the complete matching 2048-bit public key on 2026-09-10.
- User confirmed the live inquiry test worked after being asked to submit through
  the website and check the business inbox on 2026-09-10.

## Deployed changes

Production code removes Gmail from the contact link and form fallback,
sets the no-JavaScript redirect to the apex thank-you page, and aligns canonical,
Open Graph, structured data and sitemap URLs with the live apex domain.

Web3Forms responses must explicitly contain `success: true` before showing the
thank-you page; malformed or unconfirmed responses show the contact fallback.

## Verification and handoff

Production code deployed successfully in commit `a505ffb` (Actions run
`34457479437`). Local browser tests passed for confirmed, rejected, missing,
malformed and null API responses, plus mobile width and menu preselection.
All 81 local HTML references/fragments resolved.

Live browser submission could not complete: Web3Forms rejected the OPTIONS
preflight with HTTP 403 and a message about server-IP use requiring a Pro plan.
The same rejection occurs for both apex and www origins, before the form key
is sent. This test connection cannot establish inbox delivery. Do not change
providers or buy Pro on that basis. The user subsequently confirmed the live
inquiry test worked from their own browser. Do not report the automation-only
failure as proof that the customer's form is broken.

1. User creates Web3Forms form:
   - Name: Website Inquiries
   - Website URL: https://culinary-revolutionary.com/
   - Recipient: chef@culinary-revolutionary.com
2. Public Web3Forms access key installed in `index.html` on 2026-09-10.
   The online submission path is deployed and the user confirmed the test worked.
3. Receipt is user-confirmed. Reply-to behavior and outgoing message authentication
   headers have not been separately confirmed; avoid claiming those tests passed.
4. DKIM is now published and verified. DMARC was absent at the last check and
   remains recommended email hardening, not a blocker to receiving inquiries.
5. Resolve the user's previously reported Namecheap registrant contact-verification
   warning if it is still present; its current account status has not been observed.

## Decisions — do not reopen

- Business mailbox first; no temporary Gmail recipient.
- Namecheap registrar/DNS and Private Email; GitHub Pages hosting.
- No additional hosting subscription and no API eligibility purchase.
- Firecrawl requires explicit user permission. Use local/free checks where possible.
- Do not control the user's browser; the user explicitly refused that access.

## DNS record set

```text
@    A      185.199.108.153
@    A      185.199.109.153
@    A      185.199.110.153
@    A      185.199.111.153
www  CNAME  canadaornaw.github.io
@    MX 10  mx1.privateemail.com
@    MX 10  mx2.privateemail.com
@    TXT    v=spf1 include:spf.privateemail.com ~all
privateemail._domainkey TXT <matching Namecheap-generated public key; verified>
```

Check the authoritative answers before modifying any records. Do not restore Wix
nameservers now that the transfer is complete. The live inquiry test is now
user-confirmed. Retiring the old Wix subscription remains an owner account action.

## Content notes

Biography statements carry forward the client's previously published About page;
they are not independently credential-checked. Optional pricing, service-area,
insurance, certification and source-menu clarifications remain in
`CLIENT-QUESTIONS.md`; do not invent answers or treat the chosen email as undecided.
